# Tasks

## 1. Tree View Artifact Item Command

- [x] 1.1 Update `SpecsTreeItem` in `src/core/SpecsTreeDataProvider.ts` to assign `openspec-studio.openInCustomEditor` for markdown `artifact-file` items, and verify via code inspection
- [x] 1.2 Update unit tests in `test/specsTree.test.ts` to expect `'openspec-studio.openInCustomEditor'` on artifact items, and verify with `npm test`

## 2. Verification and Build

- [x] 2.1 Run build and test suite (`npm run build && npm test`) to verify clean compilation and all tests pass
